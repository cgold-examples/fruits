# 3rd party tuning:
# * https://github.com/ruslo/hunter/wiki/example.custom.config.id
# NOTE: current setting doesn't change anything, it's just an example
hunter_config(GTest VERSION ${HUNTER_GTest_VERSION})
